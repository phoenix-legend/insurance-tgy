
<%
   last_day = 31
   a4_month_counts = ::UserSystem::AishiCarUserInfo.where("aishi_yaoyue = '成功' and aishi_yaoyue_day >= ? and numbers = '4SA-1012' and aishi_yaoyue_day <= ?",Date.new(Date.today.year, Date.today.month, 1), Date.new(Date.today.year, Date.today.month, last_day)).count
   a4_today_counts = ::UserSystem::AishiCarUserInfo.where("aishi_yaoyue = '成功' and aishi_yaoyue_day = ? and numbers = '4SA-1012'", Date.today).count


   all_yyc = UserSystem::YouyicheCarUserInfo.where("youyiche_jiance = '竞拍中' and yaoyue_day >= ?",Date.new(Date.today.year, Date.today.month, 1)).count
   today_yyc = UserSystem::YouyicheCarUserInfo.where("youyiche_jiance = '竞拍中' and yaoyue_day = ?",Date.today).count


%>

测试二4444：<%=a4_today_counts%> / <%=a4_month_counts%>
<br/>
测试二yyyy：<%=today_yyc%> / <%=all_yyc%>


<style type="text/css">
    body, table {
        font-size: 12px;
    }

    table {
        table-layout: fixed;
        empty-cells: show;
        border-collapse: collapse;
        margin: 0 auto;
    }

    td {
        height: 30px;
    }

    h1, h2, h3 {
        font-size: 12px;
        margin: 0;
        padding: 0;
    }

    .table {
        border: 1px solid #cad9ea;
        color: #666;
    }

    .table th {
        background-repeat: repeat-x;
        height: 30px;
    }

    .table td, .table th {
        border: 1px solid #cad9ea;
        padding: 0 1em 0;
    }

    .yinying {
        background-color: #f5fafe;
    }

    .noyinying {
        background-color: #FFFFFF;
    }
</style>
